<?xml version="1.0" encoding="utf-8"?>
<!-- Opção "Classificação" que aparece ACIMA de "HOJE" na coluna de datas,
     enquanto um campeonato estiver selecionado. Segue o mesmo padrão visual
     das opções de data (api_item_data) para ficarem padronizadas. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:gravity="center"
    android:orientation="horizontal"
    android:padding="@dimen/_3sdp">

    <TextView
        android:id="@+id/tvData"
        android:layout_width="match_parent"
        android:layout_height="@dimen/_36sdp"
        android:background="@drawable/bg_data_item"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:gravity="center"
        android:maxLines="1"
        android:text="@string/opcao_classificacao"
        android:textColor="@color/widget_text"
        android:textSize="@dimen/_6sdp"
        android:textStyle="bold" />
</LinearLayout>